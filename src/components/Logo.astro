---
import { SITE } from 'astrowind:config';
export interface Props {
  sitename?: string;
}
const {
  sitename = SITE?.name,
} = Astro.props;
---

<span
  class="self-center ml-2 rtl:ml-0 rtl:mr-2 text-2xl md:text-xl font-semibold text-gray-900 whitespace-nowrap dark:text-white"
>
  {sitename}
</span>
